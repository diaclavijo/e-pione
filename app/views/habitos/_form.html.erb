<%= form_for([@consultation,@habito],
             remote: true ) do |f| %>
  <% if @habito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@habito.errors.count, "error") %> prohibited this habito from being saved:</h2>

      <ul>
      <% @habito.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :habito %><br>
    <%= f.select :habito, Habito::HABITOS %>
  </div>
  <div class="field">
    <%= f.label :comentario %><br>
    <%= f.text_area :comentario %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
