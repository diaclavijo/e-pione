<% content_for :top_right do %>
    <h2 class="text-muted"> <%= @patient.full_name %></h2>
    <h4>Número historia clínica: <span class="text-muted"> <%= @patient.id %> </span> </h4>
<% end %>

<% content_for :top_left do %>
    <ol class="breadcrumb">
      <li>breadcrumbs</li>
    </ol>
    <h1 class="featurette-heading"> Editar paciente <span class="text-muted"></span></h1>
<% end %>

<%= render 'form' %>

<%= link_to 'Show', @patient %> |
<%= link_to 'Back', patients_path %>
