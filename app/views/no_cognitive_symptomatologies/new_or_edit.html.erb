<!--URL consultations/1/no_cognitive_symptomatology -->
<% breadcrumb :consultation_no_cognitive_symptomatology, @consultation %>
<% content_for :top_title do %>
    Sintomatología No Cognitiva <span class="text-muted"><%= @consultation.id %></span>
<% end %>
<% content_for :title do %>
    Sintomatología No Cognitiva
<% end %>

<%= render 'form' %>

<%= render 'test_choice' %>

<%= render partial: 'shared/tests_history', locals: { consultation: @consultation ,
  page: params[:page],
  tests: @consultation.patient.no_cognitive_tests.page(params[:page]).order(created_at: :desc)} %>