<%= form_for([@consultation, @pathology],
            remote: true,
            html: {class: 'form-horizontal', role: 'form'},
            builder: ApplicationHelper::MyFormBuilder ,
            ) do |f| %>


  <div class="form-group">
    <%= f.label :pathology,"Patología" %>
    <%= f.select :pathology, Pathology::PATHOLOGIES %>
  </div>
    <div class="form-group">
        <div class="actions">
        <%= f.submit %>
      </div>
    </div>
<% end %>
